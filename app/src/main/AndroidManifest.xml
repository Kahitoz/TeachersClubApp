<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TCA"
        tools:targetApi="31">
        <activity
            android:name=".Backend5_Profile"
            android:exported="false" />
        <activity
            android:name=".Backend4_Notification"
            android:exported="false" />
        <activity
            android:name=".Backend4_LoginMessage"
            android:exported="false" />
        <activity
            android:name=".Backend3_AddJob"
            android:exported="false" />
        <activity
            android:name=".Backend2_Dashboard"
            android:exported="false" />
        <activity
            android:name=".Backend1_Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>